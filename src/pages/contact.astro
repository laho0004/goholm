---
import MainLayout from "../layouts/MainLayout.astro";
import Button from "../components/Button.astro";
---

<MainLayout title="Contact">
  <section class="py-80">
    <div class="max-w-[960px] mx-auto">
      <!-- CENTERED CONTENT COLUMN -->
      <div class="max-w-[640px] mx-auto">

        <!-- INTRO -->
        <section class="max-w-[420px] mb-40">
          <h2 class="tracking-[0.25em] mb-8">Contact us</h2>

          <p class="leading-relaxed">
            Would you like to know more? Your questions are more than welcome.
            Please feel free to email us at Göholm@.dk, contact us on +45 31 39
            30 51 or fill in the form below and we will get back to you as quickly
            as we can.
          </p>
        </section>

        <!-- FORM -->
        <form class="w-full mb-80">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-24 gap-y-16 mb-16">
            <div>
              <label class="block uppercase text-[10px] tracking-[0.25em] mb-8">
                Name
              </label>
              <input
                type="text"
                class="w-full border-b border-[0.5px] border-manor-red-500 bg-transparent py-2 focus:outline-none"
              />
            </div>

            <div>
              <label class="block uppercase text-[10px] tracking-[0.25em] mb-8">
                Phone
              </label>
              <input
                type="text"
                class="w-full border-b border-[0.5px] border-manor-red-500 bg-transparent py-2 focus:outline-none"
              />
            </div>
          </div>

          <div class="mb-16">
            <label class="block uppercase text-[10px] tracking-[0.25em] mb-8">
              Email*
            </label>
            <input
              type="email"
              class="w-full border-b border-[0.5px] border-manor-red-500 bg-transparent py-2 focus:outline-none"
            />
          </div>

          <div class="mb-24">
            <label class="block uppercase text-[10px] tracking-[0.25em] mb-8">
              Message*
            </label>
            <textarea
              rows="3"
              class="w-full border-b border-[0.5px] border-manor-red-500 bg-transparent py-2 resize-none focus:outline-none"
            ></textarea>
          </div>

          <div class="flex justify-center">
            <Button />
          </div>
        </form>

        <!-- DIRECTIONS -->
        <section class="max-w-[420px] mx-auto text-center">
          <h2 class="tracking-[0.25em] mb-16">Directions</h2>

          <p class="leading-relaxed">
            We are located 10 km south of Ronneby in Blekinge County. Göholm is
            strategically situated on the Gö Peninsula along the Blekinge coast.
          </p>

          <!-- H3 LINK -->
          <h3 class="mt-32 tracking-[0.25em] uppercase">
            <a
              href="https://www.google.com/maps/place/G%C3%B6holms+House,+372+94+Listerby,+Sverige/@56.1533277,15.3286061,17z"
              target="_blank"
              class="underline underline-offset-6 hover:opacity-70 transition"
            >
              CLICK HERE FOR DIRECTIONS
            </a>
          </h3>
        </section>

      </div>
    </div>
  </section>
</MainLayout>







